## 实际操作：如何在前端调提示词

### 步骤 1：创建工作流

在 ComfyUI 前端添加以下节点：

1. **右键点击空白处** → Add Node → TwitterChat → **Load Persona**
2. **右键点击空白处** → Add Node → TwitterChat/Debug → **Build Prompts (Debug)**
3. **右键点击空白处** → Add Node → TwitterChat/Debug → **Edit Prompts** ⭐
4. **右键点击空白处** → Add Node → TwitterChat/Debug → **Generate Tweet From Prompts (Debug)**

### 步骤 2：连接节点

```
[Load Persona]
    ↓ persona
[Build Prompts (Debug)]
    ↓ system_prompt                    ↓ user_prompt
[Edit Prompts] ← system_prompt_input  ← user_prompt_input
    ↓ system_prompt                    ↓ user_prompt
[Generate Tweet From Prompts (Debug)] ← 连接这两个输出
```

具体连接方式：
- `Load Persona.persona` → `Build Prompts.persona`
- `Build Prompts.system_prompt` → `Edit Prompts.system_prompt_input`
- `Build Prompts.user_prompt` → `Edit Prompts.user_prompt_input`
- `Edit Prompts.system_prompt` → `Generate Tweet From Prompts.system_prompt`
- `Edit Prompts.user_prompt` → `Generate Tweet From Prompts.user_prompt`

### 步骤 3：配置节点

**Load Persona 节点**：
```
persona_file: custom_nodes/comfyui-twitterchat/examples/fitness_girl_emily.json
```

**Build Prompts 节点**：
```
topic_type: 身材展示类（下拉选择）
```

**Generate Tweet From Prompts 节点**：
```
api_key: 你的 OpenAI/Claude API Key
api_base: https://api.openai.com/v1
model: gpt-4
temperature: 0.85
```

### 步骤 4：第一次运行（查看默认提示词）

1. 点击 **Queue Prompt** 按钮
2. 等待运行完成
3. 查看 **Edit Prompts** 节点的输出（会显示默认生成的提示词）

### 步骤 5：编辑提示词 ⭐

在 **Edit Prompts** 节点中找到这两个**大文本框**：

#### 📝 `system_prompt_override` 文本框
点击展开，你会看到类似这样的内容：
```
You are Emily Carter, a 25-year-old fitness influencer...

你的推文目标是吸引男性粉丝，内容风格要求:
- 适度性感暧昧，但不要太露骨
...
```

**可以直接修改！** 比如改成：
```
You are Emily Carter, a 25-year-old fitness influencer...

你的推文目标是吸引健身爱好者，内容风格要求:
- 专业激励，分享健身技巧（改这里！）
...
```

#### 📝 `user_prompt_override` 文本框
点击展开，你会看到类似：
```
请以 Emily Carter 的身份撰写一条推文...

要求:
...
6. 字数 60-150 字
...
```

**可以直接修改！** 比如改成：
```
请以 Emily Carter 的身份撰写一条推文...

要求:
...
6. 字数 30-60 字（改短了！）
7. 必须包含提问引导互动（新增要求！）
...
```

### 步骤 6：重新运行查看效果

1. 点击 **Queue Prompt** 按钮
2. 查看 **Generate Tweet From Prompts** 节点的输出
3. 对比修改前后的推文差异

---

## 💡 常见调整示例

### 示例 1：让推文更简短
在 `user_prompt_override` 中找到：
```
字数 60-150 字
```
改为：
```
字数 30-50 字
```

### 示例 2：增加互动性
在 `user_prompt_override` 末尾添加：
```
额外要求：
- 必须包含一个提问
- 引导粉丝在评论区回复
```

### 示例 3：改变语气风格
在 `system_prompt_override` 中找到：
```
- 适度性感暧昧，但不要太露骨
```
改为：
```
- 专业健身指导，激励人心
```

### 示例 4：指定具体场景
在 `user_prompt_override` 中添加：
```
场景必须是：gym, pink sports bra, doing squats
```

---

## ⚡ 快捷技巧

### 技巧 1：文本框留空 = 使用默认
如果不想修改某个提示词，就让对应的文本框留空：
- `system_prompt_override` 留空 → 使用 `system_prompt_input`
- `user_prompt_override` 留空 → 使用 `user_prompt_input`

### 技巧 2：逐步调整
- 第 1 次运行：两个 override 都留空，查看默认效果
- 第 2 次运行：只修改 `user_prompt_override`，小幅调整
- 第 3 次运行：同时修改两个，大幅调整

### 技巧 3：保存常用修改
找到好用的提示词后：
1. 复制文本框内容
2. 保存到文本文件
3. 下次直接粘贴使用

### 技巧 4：对比测试
创建两个并行分支，一个用默认提示词，一个用修改后的，对比效果。

---

## 🎯 总结

**在哪里调？**
→ **Edit Prompts 节点** 的两个大文本框：`system_prompt_override` 和 `user_prompt_override`

**怎么调？**
1. 连接工作流
2. 第一次运行查看默认
3. 在文本框中直接编辑
4. 重新运行查看效果

**调什么？**
- 推文长度
- 语气风格
- 内容要求
- 场景描述
- 任何你想改的！

---

现在重启 ComfyUI，你就可以在前端看到 **Edit Prompts** 节点了！
